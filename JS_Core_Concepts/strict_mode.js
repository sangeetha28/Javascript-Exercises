// JS engine will be strict in parsing the  code 

var person;
persom = {}; // will be created in the Global object

// If use strict was used, ReferenceError would have been thrown 

//"use strict" can also be used inside the function, always put it on the top. 
